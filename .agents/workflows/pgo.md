---
description: Push changes to git origin
---
// turbo-all
1. Add all changes to git using `git add -A`
2. Commit changes with a descriptive message using `git commit -m "chore: auto-commit via /pgo"`
3. Push changes to the remote repository using `git push`
